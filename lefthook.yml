pre-commit:
  jobs:
    - name: biome-check
      stage_fixed: true
      glob: "*.{js,cjs,mjs,jsx,ts,mts,tsx,astro,json,jsonc,css}"
      run: bunx biome check --write --no-errors-on-unmatched --files-ignore-unknown=true {staged_files}
